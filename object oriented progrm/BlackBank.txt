# Creating a website :
        #Using gitbash in windows 10:

$ pip install Django==1.11.2
$ pip install virtualenvwrapper
$ cd "Desktop/pyhton"
$ virtualenv website_project   # website_project is a folder in the pyhton directory of my system
                               # this would create a virtual env 
        
         # activating the virtualenv :

source Scripts/activate

$ pip freeze

