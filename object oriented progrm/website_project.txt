Make a Virtual Environemt:(In command prompt)

Lets call it HelloWold. All we do in a command prompt is enter ‘mkvirtualenv HelloWold’


Set Project Directory:

Now to bind our virtualenv with our current working directory we simply enter ‘setprojectdir .’


Workon:

Now you’ve got some work to do. Open up the command prompt and type ‘workon HelloWold’ to activate the environment and move into your root project folder.



Deactivate:

Let say you’re content with the work you’ve contributed to this project and you want to move onto something else in the command line. Simply type ‘deactivate’ to deactivate your environment.


######################33###########################



In Windows power shell:     ...GIT BASH:
                            ...to create virtualenv in any folder in git , navigate to any folder and use 'virtualenv [newfoldername]
-Use Scripts/activate       ...for git bash use command 'source [foldername]/Scripts/activate'   to activate virtualenv


-'pip install Django==1.10'
- pip freeze
-'django-admin.py startproject agri'     ...(If mkdir command doesnt workchange the destination of the project to c:/[anyfolder]/
                                              and then type django-admin.py startproject agri)
- 'python manage.py migrate' 
- cd c:/agri/python manage.py runserver
- Install MySQL...........(One1Life$Work%Hard>)